org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# The project lives on D: while the pub cache (plugin Kotlin sources) lives on C:.
# Kotlin's relocatable incremental caches call File.relativeTo() across those two
# roots and blow up with "this and base files have different roots", which surfaces
# as "Daemon compilation failed: null". Non-incremental Kotlin compilation avoids it.
kotlin.incremental=false
